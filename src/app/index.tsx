import { router } from 'expo-router'; import { useState } from 'react'; import { Pressable, StyleSheet, Text, View } from 'react-native'; import { SafeAreaView } from 'react-native-safe-area-context'; import { Button } from '@/components/Button'; import { Input } from '@/components/Input'; import { colors } from '@/constants/theme'; import { AuthService } from '@/services/chat';
export default function Welcome() { const [mode, setMode] = useState<'login' | 'register'>('login'); const [name, setName] = useState(''); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [role, setRole] = useState<'Atleta' | 'Profissional'>('Atleta'); const [error, setError] = useState(''); function submit() { try { const user = mode === 'register' ? AuthService.register({ name: name.trim(), email: email.trim(), password, role }) : AuthService.login(email.trim(), password); if (mode === 'register') { AuthService.login(user.email, password); router.replace('/OnboardingPreferencias'); return; } router.replace('/Dashboard'); } catch (value) { setError(value instanceof Error ? value.message : 'Não foi possível continuar.'); } } return <SafeAreaView style={styles.screen}><View style={styles.card}><Text style={styles.brand}>NEXTAGON</Text><Text style={styles.intro}>A comunidade profissional que move o fitness.</Text><View style={styles.tabs}><Tab label="Entrar" active={mode === 'login'} onPress={() => setMode('login')} /><Tab label="Criar conta" active={mode === 'register'} onPress={() => setMode('register')} /></View>{mode === 'register' ? <><Input value={name} onChangeText={setName} placeholder="Nome completo" /><View style={styles.roles}><Role label="Atleta" active={role === 'Atleta'} onPress={() => setRole('Atleta')} /><Role label="Profissional" active={role === 'Profissional'} onPress={() => setRole('Profissional')} /></View></> : null}<Input value={email} onChangeText={setEmail} placeholder="E-mail" keyboardType="email-address" autoCapitalize="none" /><Input value={password} onChangeText={setPassword} placeholder="Senha" secureTextEntry />{error ? <Text style={styles.error}>{error}</Text> : null}<Button title={mode === 'login' ? 'Entrar' : 'Criar minha conta'} onPress={submit} disabled={!email || password.length < 6 || (mode === 'register' && !name.trim())} /></View></SafeAreaView>; }
function Tab({ label, active, onPress }: { label: string; active: boolean; onPress: () => void }) { return <Pressable onPress={onPress} style={[styles.tab, active && styles.tabActive]}><Text style={[styles.tabText, active && styles.tabTextActive]}>{label}</Text></Pressable>; } function Role({ label, active, onPress }: { label: 'Atleta' | 'Profissional'; active: boolean; onPress: () => void }) { return <Pressable onPress={onPress} style={[styles.role, active && styles.roleActive]}><Text style={[styles.roleText, active && styles.roleTextActive]}>{label}</Text></Pressable>; }
const styles = StyleSheet.create({ screen: { flex: 1, justifyContent: 'center', padding: 22, backgroundColor: colors.bg }, card: { backgroundColor: colors.card, borderColor: colors.border, borderWidth: 1, borderRadius: 20, padding: 24, gap: 14 }, brand: { color: colors.text, fontSize: 32, fontWeight: '900', letterSpacing: 1 }, intro: { color: colors.text2, fontSize: 15, lineHeight: 22, marginBottom: 10 }, tabs: { flexDirection: 'row', borderBottomWidth: 1, borderColor: colors.border }, tab: { flex: 1, paddingBottom: 10, alignItems: 'center' }, tabActive: { borderBottomWidth: 2, borderColor: colors.accent }, tabText: { color: colors.text3, fontWeight: '700' }, tabTextActive: { color: colors.accent }, roles: { flexDirection: 'row', gap: 8 }, role: { flex: 1, padding: 12, borderRadius: 10, borderWidth: 1, borderColor: colors.border, alignItems: 'center' }, roleActive: { borderColor: colors.accent, backgroundColor: colors.accentDim }, roleText: { color: colors.text2, fontWeight: '700' }, roleTextActive: { color: colors.accent }, error: { color: colors.red, fontSize: 13 } });
