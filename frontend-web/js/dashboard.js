/* =============================================================
   dashboard.js — Next Agon · Painel do Usuário
   v2.0 — Delegado ao main.js (perfil unificado, idioma, favs)
   ============================================================= */

// Todas as funções de perfil (optFoto, optSenha, optIdioma,
// optFavoritos, optLogin, optNotif, optTema, optSair,
// openPanel, closePanel, initProfile) agora vivem em main.js.

// Inicialização delegada ao main.js